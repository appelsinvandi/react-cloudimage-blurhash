---
name: BackgroundImg
---

import { Playground } from 'docz'
import { BackgroundImg, CloudimageProvider, ComponentType } from '../../index.ts'

# BackgroundImg

The `<BackgroundImg>` tag will load the image, and if provided with a blurhash, show a blurred placeholde until the image is loaded.

<Playground>
  <CloudimageProvider config={{ token: 'demo' }}>
    <div
      style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(3, 1fr)',
        gridColumnGap: '8px',
        gridRowGap: '8px',
        maxWidth: '100%',
        width: '100%',
      }}
    >
      <BackgroundImg src="sample.li/hotel.jpg" type={ComponentType.WIDTH_BOUND_RATIO} ratio={1 / 1} blurhash="LYJH,LyZER9FtSNfkXo#M|oLWCkC" />

      <BackgroundImg src="https://scaleflex.ultrafast.io/https://jolipage.airstore.io/121.jpg" type={ComponentType.WIDTH_BOUND_RATIO}  ratio={1 / 1} blurhash="LFG8fyoy~A%N00vex^0z*0$eWAV@" />

      <BackgroundImg src="https://scaleflex.ultrafast.io/https://jolipage.airstore.io/img.jpg" type={ComponentType.WIDTH_BOUND_RATIO}  ratio={1 / 1} blurhash="LZE:9@tnRjM{yZxut8oftpxtWARj" />
    </div>

  </CloudimageProvider>
</Playground>
